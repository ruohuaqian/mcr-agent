overrides:
  models.model.seq2seq_im_mask_sub:
    action: "override"
    source: "mcr-agent"
    path: "models/model/seq2seq_im_mask_sub.py"

  nn.vnn_sub:
    action: "override"
    source: "mcr-agent"
    path: "models/nn/vnn_sub.py"

